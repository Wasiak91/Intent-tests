{
  "private": true,
  "scripts": {
    "test": "percy exec -- mocha tests/ --exit"
  },
  "devDependencies": {
    "@percy/puppeteer": "^1.1.0",
    "chai": "^4.2.0",
    "mocha": "^8.4.0",
    "puppeteer": "^4.0.1"
  }
}
